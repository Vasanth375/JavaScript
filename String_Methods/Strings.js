// array initialization
let array = "Bob";
console.log(array);

// strings are immutable means once initialized we can't modify the string
array[0] = "J"; 

// just re-initialized
array = "Job";

// to find the last-Nth value of the string find 2nd value of the string
lastValueIs = array[array.length-2];
// to find the length of the string use length property
// 