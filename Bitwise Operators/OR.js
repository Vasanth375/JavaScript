//The bitwise OR operation a | b returns the binary value with a 1 where either operands or both operands have 1's
// in a specific position, and 0 when both values have 0 in a position
BITWISE_OR = 1 | 5
console.log((BITWISE_OR))
